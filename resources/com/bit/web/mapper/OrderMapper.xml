<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bit.web.mapper.OrderMapper"> 

    <insert id="insertOrder" parameterType="com.bit.web.domain.OrderDTO">
        insert into ORDERS (ORDER_ID, CUSTOMER_ID, EMPLOYEE_ID, ORDER_DATE, SHIPPER_ID)
        values (#{orderId},#{customerId},#{employeeId},#{orderDate},#{shipperId})
    </insert>

    <select id="selectOrders" resultType="com.bit.web.domain.OrderDTO">
        select * from ORDERS 
    </select>

    <select id="selectOrdersByOption" resultType="com.bit.web.domain.OrderDTO" parameterType="com.bit.web.domain.OrderDTO">
      select * from ORDERS where CUSTOMER_ID = #{customerId}
    </select>

    <select id="selectOrderByOrderId" resultType="com.bit.web.domain.OrderDTO" parameterType="string">
        select * from ORDERS where ORDER_ID = #{value}
    </select> 

    <update id="updateOrder" parameterType="com.bit.web.domain.OrderDTO">
        update ORDERS set
        CUSTOMER_ID = #{customerId},
        EMPLOYEE_ID = #{employeeId},
        ORDER_DATE = #{orderDate},
        SHIPPER_ID = #{shipperId}    
        where ORDER_ID = #{orderId}
    </update>

    <delete id="deleteOrder" parameterType="com.bit.web.domain.OrderDTO">
        delete from ORDERS where ORDER_ID = #{orderId}
    </delete>

</mapper>